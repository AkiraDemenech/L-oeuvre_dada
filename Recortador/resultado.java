import java.io.FileReader(arq));
		
			while(b = false;
					
					if(words);
					cart = new " + poem.add("");
		}
		
		write("New_Poem.txt) {
		
		int c < DIM; c++;
				br;
		
		try {
				quant; c > 0) {
					list;
		
		StringBuffer();
			while(true;
				String> txt", words.size(); c++;
				br.readLine();
				
				if(words);
					cart.get(c++;
				br;
		
		try {
			
			br;
		
		try {
			
			BufferedWriter writer.close();
			}
			
		} catch (IOException e) {
				quant = 20;
		int c]);
			if(Character.isLetter(general) {
		
		List<String>> txt";
		}
		
		writer(palavras+".txt"));
				c))!=-1; c>0);
		System.out.print('.');
			base, c));
				
					if(separator.indexOf(s = br;
		
		try {
				quant = 20;
		int cartela = 9)))
					break;
				
				m) {
		
		if(m.add("=");
			
		}
		
		System.out.print("Dada");
		} while(true;
				String>> txt) {
		
		try {
			
			BufferedWriter(new FileWriter(nome) {
		
		List;
import java.io.FileReader(arq));
		
			while(true;
				
				System.out.print(" > ");
		write("New_Poem.txt", poem;
	public static int v; c-1) {
				if(s==null)
			return words);
		
		for(int c<=DIM);
		}
		
	}

}
